import { SearchPage } from "@/components/search/search-page";

export const metadata = {
  title: "Search Investment Properties — YULMIA",
  description:
    "Search houses, multifamily, and distressed investment properties in South Florida. Filter by cap rate, cash flow, and more.",
};

export default function Page() {
  return <SearchPage />;
}