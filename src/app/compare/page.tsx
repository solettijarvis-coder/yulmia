import { ComparePage } from "@/components/compare/compare-page";

export const metadata = {
  title: "Compare Properties — YULMIA",
  description:
    "Compare up to 3 investment properties side by side. Analyze cap rates, cash flow, financial details, and value indicators for South Florida real estate.",
};

export default function Page() {
  return <ComparePage />;
}
